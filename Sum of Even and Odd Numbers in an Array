Sum of Even and Odd Numbers in an Array

#include <stdio.h>

int main() {

int numbers[10], evenSum = 0, oddSum = 0;

// Input 10 numbers

for (int i = 0; i < 10; i++) {

printf("Enter number %d: ", i + 1);

scanf("%d", &numbers[i]);

}

// Calculate the sum of even and odd numbers

for (int i = 0; i < 10; i++) {

if (numbers[i] % 2 == 0) {

evenSum += numbers[i]; // Add to even sum if the number is even

} else {

oddSum += numbers[i]; // Add to odd sum if the number is odd

}

}

printf("Sum of even numbers: %d\n", evenSum);

printf("Sum of odd numbers: %d\n", oddSum);

return 0;

}
