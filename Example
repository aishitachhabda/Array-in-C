C program that reads two arrays of size 5, performs element-wise addition, and stores the results in a third
array.

#include <stdio.h>

int main() {

int arr1[5], arr2[5], sum[5]; // Declare three arrays of size 5

// Read the first array from the user

printf("Enter 5 integers for the first array:\n");

for (int i = 0; i < 5; i++) {

scanf("%d", &arr1[i]);

}

// Read the second array from the user

printf("Enter 5 integers for the second array:\n");

for (int i = 0; i < 5; i++) {

scanf("%d", &arr2[i]);

}

// Perform addition of corresponding elements and store the
result in the third array

for (int i = 0; i < 5; i++) {

sum[i] = arr1[i] + arr2[i];

}

// Print the resulting sum array

printf("The resulting array after addition is:\n");

for (int i = 0; i < 5; i++) {

printf("%d ", sum[i]);

}

return 0;

}
